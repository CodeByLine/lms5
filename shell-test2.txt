python3 manage.py shell
exit()


REQUIREMENTS.TXT:\
	pip freeze > requirements.txt
	python -m pip install -r requirements.txt


SEED DATA:
	python3 manage.py dumpdata > seed_data.json
	python manage.py loaddata seed_data.json


INSTALL: 
	pip3 install Django
	python3 -m pip install --upgrade pip

MIGRATE:
        python manage.py makemigrations
        python manage.py migrate

START:
	source ./venv/bin/activate
	python3 -m venv ./venv
	python3 manage.py runserver

Create Superuser:
        python manage.py createsuperuser

>>> u = CustomUser(email='coco@me.
com', date_of_birth='2006-12-3',password='woof')
>>> u.save()
>>> u
<CustomUser: coco@me.com>
###
>>> u1.email
'coco@me.com'

>>> u1.date_of_birth
datetime.date(2006, 12, 3)

#### BUT: to make into superuser:
# from: https://gist.github.com/Brachamul/f41ee36ae2b127b99fdfd1e81d4b9f6e

>>> from slm_app.models import CustomUser
>>> from slm_app.managers import CustomUserManager
from django.contrib.auth import get_user_model
CustomUser = get_user_model()
user = CustomUser.objects.get(email="coco@me.com")
user.is_staff = True
user.is_admin = True
user.is_superuser = True
user.save()

### Also need to reset password (check the 'sent_mail' folder)

>>> from slm_app.models import Course
>>> c = Course.objects.all()
>>> c
<QuerySet []>
>>> c1 = Course(course_name="Math")
>>> c1
<Course: Math>
>>> c1.save()
